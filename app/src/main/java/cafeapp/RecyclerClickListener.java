package cafeapp;

public interface RecyclerClickListener {
    /**
     * Allows user to select items from the RecyclerView by clicking an entry.
     * @param position Position of the selected item from the RecyclerView
     */
    void DonutClick(int position);
}
